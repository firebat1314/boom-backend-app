import { GetGameNameByTypePipe } from './get-game-name-by-type.pipe';

describe('GetGameNameByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GetGameNameByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
